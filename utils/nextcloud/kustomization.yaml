apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - repo: https://nextcloud.github.io/all-in-one
    debug: true
    valuesFile: values.yaml
    name: nextcloud-aio-helm-chart

