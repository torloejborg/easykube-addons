apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
 - manifests/ingress-controller.yaml
 - manifests/default-cert.yaml
 - manifests/configmap.yaml

#patches:
# - path: manifests/rootless.json
#   target:
#    kind: Deployment
